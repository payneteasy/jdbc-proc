create table employee (
     employee_id   int(10) unsigned not null auto_increment
   , firstname     varchar(60)
   , lastname      varchar(60)
   , company_company_id   varchar(60)
   
   , constraint pk_employee primary key (employee_id)
) engine = innodb ;

